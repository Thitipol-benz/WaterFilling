<!DOCTYPE html>
<html>
<head>
    <title>แสดงเวลาและวันที่</title>
</head>
<body>
    <div id="datetime"></div>
    <script>
        // สร้างฟังก์ชันสำหรับแสดงเวลาและวันที่
        function displayDateTime() {
            // สร้างวัตถุ Date เพื่อเข้าถึงข้อมูลเวลาปัจจุบัน
            var now = new Date();

            // ดึงข้อมูลเวลาและวันที่ออกมา
            var date = now.toLocaleDateString(); // วันที่
            var time = now.toLocaleTimeString(); // เวลา

            // แสดงข้อมูลเวลาและวันที่ใน element HTML ที่มี id="datetime"
            document.getElementById('datetime').innerHTML = 'วันที่: ' + date + '<br>เวลา: ' + time;
        }

        // เรียกใช้ฟังก์ชัน displayDateTime() เพื่อแสดงค่าเวลาและวันที่เมื่อโหลดหน้าเว็บ
        displayDateTime();

        // ปรับเวลาทุก 1 วินาที
        setInterval(displayDateTime, 1000);
    </script>
</body>
</html>
